name: V2 Foundation Tests

on:
  push:
    branches:
      - v2-foundation
  pull_request:
    branches:
      - main
      - v2-foundation

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install test dependencies
        run: python -m pip install --upgrade pip pytest pandas openpyxl
      - name: Run V2 foundation tests
        run: pytest -q v2/tests
